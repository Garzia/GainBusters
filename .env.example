# STORAGE_MODE: How the application database JSON state is saved.
# Set to 'local' for self-hosted local JSON file backend storage (Docker).
# Set to 'browser' (or leave empty) for browser-based client-side localStorage.
STORAGE_MODE=local

